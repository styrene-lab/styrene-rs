# Tasks

## 1. Fallback gate

- [x] 1.1 Accept the Preparing phase in the fallback gate, with a unit test
- [x] 1.2 Keep the direct error in the failure when the fallback is refused, at both call sites
