[package]
name = "styrene-telemetry"
version = "0.1.0"
edition = "2021"
rust-version = "1.75"
license = "MIT"
description = "Styrene mesh telemetry schema — typed observations over LXMF FIELD_TELEMETRY"
repository = "https://github.com/styrene-lab/styrene-rs"

[features]
default = []
alloc = []
std = ["alloc", "ciborium/std"]
tokio = ["std"]

[dependencies]
heapless = { version = "0.8", default-features = false, features = ["serde"] }
serde = { version = "1", default-features = false, features = ["derive"] }
ciborium = { version = "0.2", default-features = false }
ciborium-io = { version = "0.2", default-features = false }

[dev-dependencies]
serde_json = "1"
tokio = { version = "1", features = ["full"] }
