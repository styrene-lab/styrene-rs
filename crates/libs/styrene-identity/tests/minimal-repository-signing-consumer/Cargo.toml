[package]
name = "minimal-repository-signing-consumer"
version = "0.0.0"
edition = "2024"
rust-version = "1.97"
publish = false

[dependencies]
styrene-identity = { path = "../..", default-features = false, features = ["repository-signing"] }

[workspace]
