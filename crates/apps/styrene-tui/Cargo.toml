[package]
name = "styrene-tui"
version = "0.3.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Styrene terminal UI spike — Ratatui mesh status display"
build = "build.rs"
publish = false

[[bin]]
name = "styrene-tui"
path = "src/main.rs"

[dependencies]
# TUI core
ratatui = "0.30"
crossterm = "0.29"
ratatui-textarea = { version = "0.8", features = ["crossterm"] }
tui-tree-widget = "0.24"
ratatui-toaster = "0.1"
tachyonfx = { version = "0.25.0", features = ["sendable"] }
tui-syntax-highlight = "0.2"
unicode-width = "0.2"

# Protocol crates (no transport — pure data types)
styrene-rns = { workspace = true }
styrene-micron = { workspace = true }

# Daemon integration
styrene-ipc = { path = "../../libs/styrene-ipc", version = "0.1.0" }
styrene-ipc-wire = { path = "../../libs/styrene-ipc-wire", version = "0.1.0" }
styrene-ipc-client = { path = "../../libs/styrene-ipc-client", version = "0.1.0" }
styrene-session = { path = "../../libs/styrene-session", version = "0.1.0" }
styrened = { path = "../styrened", version = "0.3.0" }

# Utilities
hex.workspace = true
toml = { workspace = true }
rand_core = { workspace = true, features = ["getrandom"] }
serde.workspace = true
serde_json.workspace = true
rmpv = { workspace = true, features = ["with-serde"] }
rmp-serde.workspace = true
tokio = { workspace = true }
anyhow.workspace = true

[lints]
workspace = true

[dev-dependencies]
tempfile.workspace = true
