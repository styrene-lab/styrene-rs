apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: styrene

resources:
  - namespace.yaml
  - configmap.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml

labels:
  - pairs:
      app.kubernetes.io/part-of: styrene-mesh
    includeSelectors: false
